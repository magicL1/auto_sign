{
  "name": "auto-sign",
  "version": "1.0.0",
  "private": true,
  "description": "Juejin daily automation on GitHub Actions",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/juejin.js && node --check src/cookies.js && node --check src/failure.js && node --check scripts/record-failure.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "1.62.0"
  }
}
